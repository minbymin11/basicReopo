/*
GUI 프로그램방법
	1. 컨테이너준비(Frame,Dialog,Window)
   	2. 컴포넌트 준비 (Button,List,Canvas,TextArea,TextField)
	3. 컨테이너에 컴포넌트를 부친다(add)
	4. 컨테이너의 크기설정
	5. 컨테이너 보여주기.
*/
package basic;

import java.awt.Color;
import java.awt.Frame;

public class AwtMain {

	public static void main(String[] args) {
/*
 * Constructs a new, initially invisible Frame object with the specified title.
 */
		Frame f = new Frame("나의 첫번째 프레임"); // 타이틀
		f.setSize(300, 400); //사이즈 
		f.setBackground(new Color(0, 0, 255));
		f.setTitle(f.getTitle()+"변경");//타이틀 변경
		f.setVisible(true); //invisible frame을 보여주는 메소드
		
	}

}
